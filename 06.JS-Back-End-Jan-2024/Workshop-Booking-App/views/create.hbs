<section id="create-section" class="">

  <h1 class="item">New Accommodation</h1>

  <main class="item padded align-center">

    <form method="post" action="/create" class="layout left large">

      {{#if error}}
      <div class="error-box">
        {{#each error.messages}}
        <p>{{this}}</p>
        {{/each}}

      </div>
       {{/if}}

      <div class="col aligned">
        <label><span>Name:</span>
          <input {{#if error.fields.name}}class="field-error"{{/if}}type="text" id="name" name="name" value="{{body.name}}"/></label>
        <label><span>Location:</span>
          <input {{#if error.fields.city}}class="field-error"{{/if}}type="text" id="city" name="city" value="{{body.city}}" /></label>
        <label><span>Bads</span>
          <input {{#if error.fields.bads}}class="field-error"{{/if}}type="number" id="beds" name="beds" value="{{body.bads}}"/></label>
        <label><span>Price:</span>
          <input {{#if error.fields.price}}class="field-error"{{/if}}type="number" id="price" name="price" value="{{body.price}}"/></label>
        <label><span>Image URL:</span>
          <input {{#if error.fields.imgUrl}}class="field-error"{{/if}}type="text" id="city" name="imgUrl" value="{{body.imageUrl}}" /></label>
      </div>

      <div class="content pad-med align-center vertical">
        <label>
          <span>Description</span>
          <textarea {{#if error.fields.description}}class="field-error" {{/if}}
            name="description">{{body.description}}</textarea>
        </label>

        <div class="align-center">
          <input class="action" type="submit" value="Publish Item">
        </div>
      </div>

    </form>
  </main>

</section>