<section id="edit-section" class="">

  <h1 class="item">Edit Accommodation</h1>
  
  <main class="item padded align-center">

    <form method="post" action="/edit/{{room._id}}" class="layout left large">

      {{#if error}}
      <div class="error-box">
        {{#each error}}
        <p>{{this}}</p>
        {{/each}}
      </div>
      {{/if}}

      {{#with room}}
      <div class="col aligned">
        <label><span>Name:</span>
          <input type="text" id="name" name="name" value="{{name}}" /></label>
        <label><span>Location:</span>
          <input type="text" id="city" name="city" value="{{city}}" /></label>
        <label><span>Bads</span>
          <input type="number" id="beds" name="beds" value="{{beds}}" /></label>
        <label><span>Price:</span>
          <input type="number" id="price" name="price" value="{{price}}" /></label>
        <label><span>Image URL:</span>
          <input type="text" id="city" name="imgUrl" value="{{imgUrl}}" /></label>
      </div>

      <div class="content pad-med align-center vertical">
        <label>
          <span>Description</span>
          <textarea class="field-error" name="description">{{description}}</textarea>
        </label>

        <div class="align-center">
          <input class="action" type="submit" value="Update Item">
        </div>
      </div>
      {{/with}}
    </form>

  </main>

</section>